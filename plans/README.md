# Animation improvement plans

Generated by `improve-animations` (standard-effort audit) against commit `a68fcf2` on branch `design/fase-polish-home`. Each plan is self-contained — see the individual file for exact code, values, and verification steps. None of these have been applied yet; run each with `improve-animations execute <plan>` (or hand to any executor) one at a time, following the order below.

| # | Title | Severity | Category | Status | Depends on |
| --- | --- | --- | --- | --- | --- |
| [001](001-gate-hover-on-pointer-fine.md) | Gate all `hover:` motion behind `(hover: hover) and (pointer: fine)` | HIGH | Accessibility | **DONE** | — |
| [002](002-scope-transition-all.md) | Replace `transition-all` with scoped `transition` | MEDIUM | Performance | **DONE** | — |
| [003](003-reduced-motion-scroll-hint-pulse.md) | Respect `prefers-reduced-motion` for scroll hint and terracotta pulse | MEDIUM | Accessibility | **DONE** | — |
| [004](004-mobile-menu-ease-out.md) | Fullscreen mobile menu: `ease-in-out` → strong `ease-out` | MEDIUM | Easing & duration | **DONE** | — |
| [005](005-consolidate-easing-tokens.md) | Consolidate hand-typed cubic-béziers into `--ease-out` token | LOW | Cohesion & tokens | **DONE** | Should run **after** 004 (checks whether 004's arbitrary-value class needs a note as an accepted exception) |
| [006](006-animate-faq-details.md) | Animate the FAQ `<details>`/`<summary>` open/close | MEDIUM | Missed opportunity / Interruptibility | **DONE** | — |
| [007](007-extend-arrow-affordance-related-cards.md) | Extend Home's arrow-reveal hover to "También te puede interesar" cards | LOW | Cohesion / Missed opportunity | **DONE** | Best run **after** 002 (its Boundaries note checks current `transition-all`/`transition` state before editing) |
| [008](008-floating-buttons-entrance.md) | Give phone/WhatsApp floating buttons an entrance | LOW | Missed opportunity | **DONE** | Best run **after** 003 and 005 (consolidates into the same reduced-motion block, reuses `--ease-out`) |

**All 8 plans executed and verified on branch `design/fase-polish-home`** (commits `8de3c55`, `ceb4ed4`, `2475593`, `2367aaf`, `020c230`, `fdecd88`). Not yet merged to `main` — pending the usual pre-merge checklist (isolated worktree rebuild, 3-breakpoint preview, production validation).

## Recommended execution order

1. **001** — zero HTML risk (one config file + rebuild), fixes the highest-severity finding, touches nothing else.
2. **002** — mechanical, low-risk, sitewide. Do this early since plan 007 checks its outcome.
3. **004** — small, isolated, single element.
4. **005** — formalizes the token plan 004 reuses; run right after it.
5. **003** — small, isolated, single file.
6. **008** — reuses conventions from 003 and 005; run after both.
7. **006** — the biggest plan (15 files + 1 new shared script), independent of the others, but best done once the smaller ones are proven out.
8. **007** — the widest-reaching plan (13 files × 3 cards = 39 edits), lowest severity; do last.

Every plan already carries its own "if the code doesn't match what's cited here, STOP and report" boundary, so running them out of this order is not unsafe — this ordering just minimizes rework (e.g. running 005 before 004 would mean re-checking the token file a second time).

## Notes

- All 8 plans target the `design/fase-polish-home` branch (or whatever branch is current when execution starts) — none of them should be applied directly to `main`, per this project's established git discipline: one phase, verified in an isolated worktree with a rebuilt CSS and a 3-breakpoint preview, before any merge.
- Plans 001 and 002 require `npm run build` after editing (Tailwind classes/config). Plans 003–008 are hand-written CSS/JS in `<style>`/`<script>` blocks or new static files — no build step, but still verify in a real browser per each plan's Feel check.
- No plan touches JSON-LD, meta tags, tracking `onclick` handlers, link destinations, or visible copy — confirmed per-plan in each Boundaries section, consistent with this project's standing rule to never let a design/motion pass change protected content.
